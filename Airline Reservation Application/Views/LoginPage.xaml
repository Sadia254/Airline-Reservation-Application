<Page
    x:Class="Airline_Reservation_Application.Views.LoginPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Airline_Reservation_Application.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Image 
                    Source="/Assets/NASA.jpg" 
                    Stretch="UniformToFill" 
                    HorizontalAlignment="Center" 
                    VerticalAlignment="Center" />
        <StackPanel>
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="UserName:"/>
            <TextBox Name="LoginTextBox" Text="admin"/>

        </StackPanel>

        <StackPanel HorizontalAlignment="Center" >
            <TextBlock Text="Password:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox x:Name="PassTextBox" Text="asdf"/>

        </StackPanel>
            <Button Name="AcceptBtn" Content="Login" Click="AcceptBtn_Click" HorizontalAlignment="Center"></Button>
        </StackPanel>
    </Grid>
</Page>
